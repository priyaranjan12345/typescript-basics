// tuples
// it restrict order of data and no of data it required
let user: [string, number, boolean]

user = ["Ram", 1, true]

let rgb: [number, number, number] = [255, 255, 255]

type Account = [string, number]
let newAccount: Account = ["Sita", 10]
newAccount[1] = 2 // it can override value

export { }